# MOLTBOT Environment Configuration (FREE TOOL EDITION)

# Brain Settings
OLLAMA_BASE_URL=http://localhost:11434
# No GROQ key needed - local Ollama prioritized

# Email Ops (Required for email_ops skill)
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# Messaging (Selenium WhatsApp)
# No keys needed - manually scan QR code on first run

# Vision (Multimodal)
# Uses local 'llava:latest' via Ollama

# Safety
OFFLINE_MODE=True
MASK_SENSITIVE_INFO=True
